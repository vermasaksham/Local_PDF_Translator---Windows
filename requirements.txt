# Runtime dependencies.
#
# PySide6-Essentials rather than the full PySide6: the extras (WebEngine, Qml,
# Charts, 3D) add roughly 300 MB and none of them are used.
PySide6-Essentials>=6.6,<7
PyMuPDF>=1.24,<2
ctranslate2>=4.0,<5
sentencepiece>=0.2
